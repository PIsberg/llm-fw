node_modules/
.llm-fw.json
dist/
*.local
.env
.env.*
!.env.example
coverage/
.nyc_output/
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.llm-fwrc.json
proxy-log.txt
test-results/
test-proxy.js

# Local scratch / probe scripts and backups (never committed)
scratch/

# Stryker mutation testing (Task C9) — local run artifacts, not committed.
# The baseline score + surviving-mutant clusters are recorded in
# docs/TESTING.md instead.
.stryker-tmp/
reports/mutation/
