# AgentDojo integration — pin to the version this adapter targets.
#
# AgentDojo's internal module paths (agent_pipeline, benchmark, task_suite)
# have drifted across releases; run_agentdojo.py and pipeline.py are written
# against the version below. If you bump it and imports break, see the guarded
# error messages in those files for the exact symbols to re-point.
agentdojo==0.1.23
httpx>=0.27

# The agent LLM client(s) AgentDojo calls — install whichever matches --model,
# and set the corresponding API key (OPENAI_API_KEY / ANTHROPIC_API_KEY / …).
# AgentDojo pulls these in transitively; listed here as a reminder.
# openai>=1.0
# anthropic>=0.40
